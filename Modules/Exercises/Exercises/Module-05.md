Exercise 1 — User Input

Create a C# program that:

Takes the user's name as input.
Takes the user's age as input.
Displays both values.
Exercise 2 — Multiple Inputs

Create a C# program that:

Takes two numbers as input.
Displays both numbers.
Exercise 3 — Formatted Output

Create a C# program that takes:

Name
Age
City

Then displays them in a clean format.

Exercise 4 — Student Information

Create a C# program that takes:

Student Name
Roll Number
Marks

Then displays all information using string interpolation.
